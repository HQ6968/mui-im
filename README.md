# mui-im

```

git init 
git remote add origin  git@github ...

git add - A  
git commit -m 'xxx'

git push origin master 
git pull origin master
```

#冲突
```
git stash   备份 回退
git pull origin master  // 拉取远程修改
git stash list  查看备份记录
git stash pop   还原记录 手动修改冲突
```

